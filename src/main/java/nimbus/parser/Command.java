package nimbus.parser;

/**
 * Represents the different commands that can be issued to the Nimbus chatbot.
 */
public enum Command {
    LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, BYE, UNKNOWN, FIND
}
